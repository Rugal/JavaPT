machine:
  java:
    version: oraclejdk8

database:
  override:
    # replace CircleCI's generated database.yml
    - cp main/resources/jdbc_circleci.properties main/resources/jdbc.properties
    - psql -f 'db/jpt.sql' -U ubuntu